import std.io

enum TokenKind {
    LeftParenthesis,
    RightParenthesis,
    Plus,
    Minus,
    Star,
    Slash,

}

fun main() : int {
    let token = TokenKind.LeftParenthesis

    if token == TokenKind.RightParenthesis {
        io.puts("true")
    }
    else {
        io.puts("false")
    }
}